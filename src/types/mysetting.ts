export type Category = {
  id: string;
  name: string;
  type: string;
  is_deleted: boolean;
  description: string;
  registration_date: string;
  amount: string;
};

export type typeSelect = {
  value: string;
  label: string;
};
